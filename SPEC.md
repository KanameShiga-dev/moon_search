# 月をさがす — 仕様書

スマートフォンのカメラ映像に重ねて、月の方向を矢印で示すWebアプリ。中秋の名月（2026-09-25）の観月用に作成。現行実装は `index.html` 1ファイルで完結している。

この文書は、実装の意図・計算方法・受け入れ基準をまとめたもの。改修時はこの仕様を正とし、仕様を変える場合は本書も同時に更新すること。

---

## 1. 目的と利用シーン

- 利用者はスマホを空にかざし、画面の案内に従って月を見つける。
- 月が画面内にあればリングで位置を示し、画面外なら画面端の矢印で方向を示す。
- 月が地平線の下にある時間帯でも、方向と月の出の時刻を示す。

## 2. 対象環境

| 項目 | 要件 |
|---|---|
| 主対象 | iPhone（iOS 16以降）のSafari。端末は縦持ちを想定 |
| 副対象 | AndroidのChrome（`deviceorientationabsolute` 対応端末） |
| 配信 | HTTPS必須（カメラ・位置情報・モーションAPIの前提条件） |
| 依存 | 外部ライブラリなし。Google Fontsのみ（失敗時はシステムフォントに代替） |
| 埋め込み | iframe内ではカメラ等が拒否される場合がある。単独ページとして開く運用を想定 |

## 3. 機能要件

| ID | 要件 |
|---|---|
| F1 | 起動画面の「月をさがす」ボタンで、モーション→位置情報→カメラの順に権限を要求する |
| F2 | 背面カメラの映像を全画面に表示する（`object-fit: cover`） |
| F3 | 現在地と現在時刻から、月の方位角・高度を計算する（15秒ごとに更新） |
| F4 | 端末の姿勢から、カメラが向いている方向を求める（毎フレーム） |
| F5 | 月がカメラの視野内にあれば、画面上の該当位置にリングを描く |
| F6 | 視野外なら、画面端に月の方向を指す矢印と、中心からの角距離（度）を描く |
| F7 | 案内文を表示する：「右へ n°　上へ m°」／「月は正面です」（角距離3°未満）／「月は地平線の下です」 |
| F8 | 月の方位（度と16方位名）、高度、輝面比（%）、今後26時間以内の月の出・月の入り時刻を表示する |
| F9 | 権限拒否・センサー不在・コンパス精度低下を画面上部の警告欄で知らせる |
| F10 | 当日（JST）の旧暦の日付と月の呼び名を、起動画面とパネルに表示する。7日以内に暦の名月があれば「あと n 日」を添える（5.6） |

## 4. 画面構成

```
┌──────────────────────────┐
│ 月をさがす 20:14             │ ← hud-top（時刻）
│ [警告欄：必要時のみ]          │ ← #warn
│                              │
│   ▶                  (カメラ映像)
│          ⊕  ← 中央照準         │
│      ◎ 月  ← 視野内の月リング   │
│                              │
│ ┌──────────────────────┐ │
│ │ 右へ 34°　上へ 12°        │ │ ← #guide
│ │ 方位 / 高度 / 輝いている面  │ │
│ │ 旧暦8月15日　中秋の名月・十五夜│ │ ← #koyomi
│ │ 月の出 16:39　月の入り 翌04:40│ │
│ │ 現在地（35.68, 139.77）    │ │
│ └──────────────────────┘ │ ← .panel
└──────────────────────────┘
```

- 起動画面 `#start`：月を模した円、見出し `#title`（当日の月の呼び名。5.6）、日付 `#date`（漢数字）、暦の行 `#startKoyomi`（旧暦日付・呼び名・「あと n 日」）、説明文、開始ボタン、権限に関する注記。
- 配色トークン：夜の紺（`--night`）、月白（`--moon`）、金（`--gold`、矢印とリング）、薄（`--susuki`、補助情報）。
- フォント：見出しは Shippori Mincho、本文と数値は Zen Kaku Gothic New。
- セーフエリア：`env(safe-area-inset-*)` で上下の余白を確保する。

## 5. 処理仕様

### 5.1 月の位置（天文計算）

簡略式（Meeus系の低精度式、suncalcと同系統）。目標精度は約0.5°。

1. J2000からの日数 `d = ms/86400000 − 0.5 + 2440588 − 2451545`
2. 月の黄道座標
   - `L = 218.316 + 13.176396·d`、`M = 134.963 + 13.064993·d`、`F = 93.272 + 13.229350·d`（度）
   - 黄経 `l = L + 6.289·sin M`、黄緯 `b = 5.128·sin F`、距離 `dist = 385001 − 20905·cos M`（km）
3. 黄道傾斜角 23.4397° で赤経・赤緯へ変換
4. 恒星時 `θ = 280.16 + 360.9856235·d + 経度`、時角 `H = θ − 赤経`
5. 高度・方位角を求める（方位は北=0°、時計回り）
6. 補正
   - 視差：`h −= asin(6378.14/dist · cos h)`
   - 大気差（Saemundsson式）：`R[分] = 1.02 / tan(h + 10.3/(h + 5.11))`。`h > −2°` のときだけ適用する

輝面比は太陽の簡略座標から位相角を求め、`(1 + cos i)/2` で計算する。

月の出・入りは、現在から26時間先まで5分刻みで高度を評価する。高度の符号が変わった区間を線形補間して時刻を求める。

### 5.2 座標系

- 地上座標：x=東、y=北、z=天頂。
- 端末座標（W3C DeviceOrientation）：x=画面右、y=画面上、z=画面から手前。背面カメラの向きは −z。
- 回転行列：`R = Rz(α)·Rx(β)·Ry(γ)`（端末座標から地上座標への変換）。
- カメラの向き `cam = R·(0,0,−1)`。端末上端の向き `top = R·(0,1,0)`。

### 5.3 方位の補正（iOS）

iOSの `alpha` は起動時を基準にした相対値なので、真北に合わせる補正が必要になる。

- 基準にするベクトル：`cam` の水平成分が0.7より大きい（端末を立てている）ときは `cam`、そうでないときは `top` を使う。
- 補正値のサンプルは `webkitCompassHeading − 基準ベクトルの方位`。
- ±180°の不連続を避けるため、補正値は単位ベクトルに変換してから平滑化する（係数0.1）。
- Androidで `deviceorientationabsolute` を受信できた場合は補正不要（補正値=0）。

### 5.4 画面への投影

1. 月の方向ベクトルを、補正値を差し引いた地上座標で求め、`Rᵀ` で端末座標 `d` に変換する。
2. `d` は係数0.25で指数平滑化し、正規化する。
3. 画面の回転角 θ（`screen.orientation.angle`）を反映する：`sx = dx·cosθ − dy·sinθ`、`sy = dx·sinθ + dy·cosθ`。
4. 焦点距離はピクセル単位で `f = 表示上の映像の長辺 / (2·tan(FOV_LONG/2))`。`FOV_LONG = 67°` は広角カメラの長辺方向の画角。
5. `dz < 0`（カメラの前方）なら `px = cx + f·sx/(−dz)`、`py = cy − f·sy/(−dz)` を求める。この点が表示枠内にあればリングを描く。
6. それ以外の場合は、画面中心から方向 `(sx, −sy)` へ延ばした線と表示枠の交点に矢印を描く。表示枠は左右44px、上110px、下はパネル上端から36px内側。月が真後ろ近くで方向が定まらないときは下向きの矢印にする。

### 5.5 案内文

- 方位差 `ΔAz = wrap180(月の方位 − カメラの方位)`、高度差 `ΔAlt = 月の高度 − カメラの高度`。
- 絶対値が2°以上の成分だけを表示する。正の値は「右／上」、負の値は「左／下」。
- 月の高度が0°未満のときは「月は地平線の下です」を優先して表示する。

### 5.6 暦（旧暦の日付と月の呼び名）

日付の区切りは利用者の地域によらず日本時間（JST）とする。

1. 朔弦望の時刻：Meeus『Astronomical Algorithms』49章の式（惑星による補正項は省略）。ΔT = 69秒。
2. 太陽の視黄経：同25章の低精度式。
3. 旧暦の日：当日を含む朔（新月）のJST日付を1日目として数える。
4. 旧暦の月：その朔の日から次の朔の前日までに含まれる中気で決める（春分0°→2月、秋分180°→8月、霜降210°→9月、小雪240°→10月。一般に `中気の黄経/30 + 2` を12で折り返す）。中気を含まない月は閏月とし、前の月と同じ番号に「閏」を付ける。
5. 呼び名の優先順
   - 暦の名月（閏月を除く）：旧8月14日「待宵」、8月15日「中秋の名月・十五夜」、9月13日「十三夜（後の月）」、10月10日「十日夜（とおかんや）」
   - それ以外の日の呼び名：2日 二日月、3日 三日月、13日 十三夜月、14日 小望月、15日 十五夜、16日 十六夜、17日 立待月、18日 居待月、19日 寝待月、20日 更待月
   - 朔・上弦・望・下弦の時刻がその日（JST）に入れば「新月」「上弦の月」「満月」「下弦の月」を併記する
6. 見出し `#title` には、暦の名月の短い名前（中秋・十三夜など）→月相→日の呼び名→「月」の順で最初にあるものを表示する。
7. 当日を含まない7日以内に暦の名月（待宵を除く）があれば「明日は◯◯」「◯◯まであと n 日」を表示する。
8. 起動画面は読み込み時に計算する。パネルは `updateMoon` の周期で再計算し、JSTの日付が変わったときだけ表示を更新する。パネルでは括弧の中の補足を省く。

## 6. 権限とフォールバック

| 状況 | 挙動 |
|---|---|
| 権限要求の順序 | `DeviceOrientationEvent.requestPermission()` をクリック直後に最初に呼ぶ（iOSではユーザー操作の直後でないと失敗するため） |
| 位置情報が取得できない | 東京（35.68, 139.77）で計算し、警告欄とパネルに「東京で仮定」と表示する |
| カメラが使えない | 背景は紺のままで、矢印・数値による案内を続ける |
| 2.5秒たってもモーションイベントが来ない | 「スマートフォンで開いてください」と警告する |
| `webkitCompassAccuracy` が負の値、または25°を超える | 8の字を描くように動かして補正するよう促す |

## 7. 受け入れ基準

### 7.1 計算の検証値

東京（35.68, 139.77）での現行実装の出力を下表に示す。国立天文台暦計算室の値との差が方位・高度とも±1°以内であること。

| 日時（JST） | 方位 | 高度 | 輝面比 |
|---|---|---|---|
| 2026-09-25 17:00 | 98.6° | 3.7° | 97.7% |
| 2026-09-25 18:00 | 107.4° | 15.2° | 97.8% |
| 2026-09-25 20:00 | 130.0° | 36.5° | 98.0% |
| 2026-09-25 23:00 | 189.6° | 50.5° | 98.4% |

- 月の出：2026-09-25 16:39頃。月の入り：2026-09-26 04:40頃（±3分以内）。

### 7.2 暦の検証値

国立天文台暦計算室の公表値との比較（2026-09-24 に確認）。

| 項目 | 基準 | 結果 |
|---|---|---|
| 2026年の朔弦望 50件（暦要項） | ±5分以内 | 最大 1.7分 |
| 2026年の二十四節気 24件の太陽黄経（暦要項） | ±0.02°以内 | 最大 0.009° |
| 中秋の名月の日付 2020〜2050年（暦Wiki） | 日付が一致 | 31年中30年一致（2033年は8節を参照） |

- 2026年の表示例：9/24 待宵、9/25 中秋の名月・十五夜、9/27 立待月・満月、10/23 十三夜、11/18 十日夜。

### 7.3 実機での確認

- 縦持ちで実際の月に向けたとき、リングの中心と月の像のずれが画面中央付近で5°以内に収まる。
- 端末を一回転させる間、矢印が常に月の方向を指し、途中で反転したり飛んだりしない。
- 端末を立てた状態と水平に寝かせた状態を行き来しても、方位補正が破綻しない。

## 8. 既知の制約

- 簡略式のため、月の位置に最大0.5°程度の誤差がある。
- 画角を定数で仮定しているため、画面の端ほど投影位置がずれる。
- 方位の精度は磁気コンパスに依存する。金属や磁石の近くではずれる。
- 横持ちの回転は計算上考慮しているが、画面のレイアウトは縦持ちに最適化している。
- 旧暦の月は「中気を含まない月を閏月」とする規則だけで決めている。この規則で閏月が定まらない「旧暦2033年問題」の期間（2033年8月〜2034年初め）は、月番号が国立天文台の扱いと異なる（2033年の中秋を10/7と判定する。国立天文台は9/8）。
- 暦の日付はJSTの暦日で切り替わる。中秋の夜でも0時を過ぎると翌日（十六夜）の表示になる。
- 朔や中気が0時の数分以内にある日は、計算誤差（数分）により日付が1日ずれる可能性がある。

## 9. 改修の候補（未着手）

優先度の高い順に並べる。

1. 天文計算を `astro.js` に分離し、7.1の検証値でユニットテストを追加する（Node.jsの標準テストランナー）。
2. 画角の校正機能：実際の月にリングを合わせる操作で `FOV_LONG` を補正し、`localStorage` に保存する。
3. 月の出前の時間帯に、月が昇る方位を地平線上にマークして表示する。
4. 時刻を指定して、任意の時刻の月の位置を確認できるモードを追加する。
5. ネイティブアプリへの移植（Swift + ARKit / CoreLocation）。姿勢推定と方位の精度向上が見込める。

## 10. ファイル構成

```
tsuki-sagashi/
├── index.html   アプリ本体（HTML/CSS/JSを1ファイルに同梱。<head> にOGP・Xカード用のメタタグ）
├── og-image.png SNSでシェアしたときのカード画像（1200×675）
├── SPEC.md      本仕様書
└── CLAUDE.md    Claude Code用の作業ルール
```
