# CLAUDE.md

## プロジェクト
「月をさがす」：スマートフォンのカメラ映像に重ねて、月の方向を矢印で示すWebアプリ。仕様は `SPEC.md` を正とする。

## 作業ルール
- 改修前に `SPEC.md` を読むこと。仕様を変えるときは、同じ変更の中で `SPEC.md` も更新する。
- 外部ライブラリは追加しない。追加が必要な場合は、理由を添えて先に提案する。
- 天文計算（SPEC 5.1）を変更したら、SPEC 7.1の検証値との差を報告する。
- 座標変換（SPEC 5.2〜5.4）は符号を間違えやすい。変更後は「矢印が月の方向を指す」ことを、計算例で確かめてから報告する。
- UIの文言は日本語。短く、動作を具体的に書く。

## 確認方法
- ローカル確認：`npx serve .` などで配信する。実機での確認にはHTTPSが必要（例：`npx localtunnel`、Cloudflare Tunnel）。
- 計算だけの確認：天文計算の関数を取り出し、Node.jsで実行する。
